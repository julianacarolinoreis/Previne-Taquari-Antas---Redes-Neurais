# LEGADO / NÃO ATIVO
#
# O GitHub só executa workflows em .github/workflows/. Este arquivo é mantido
# como ponte documental para versões antigas do projeto e NÃO deve ser usado
# para publicar o feed.
#
# Workflow canônico:
#   .github/workflows/previsao-ao-vivo.yml
# Robô canônico:
#   previne/robo/gerar_previsao_ao_vivo.py
#
# A definição canônica contém validação de schema, hashes dos modelos,
# dependências fixadas, timeouts, retries de push, gatilhos redundantes e
# proteção de frescor. Não duplique essa lógica aqui.

name: LEGADO - usar workflow canonico de Santa Tereza

on:
  workflow_dispatch:

jobs:
  aviso:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Arquivo legado. Use .github/workflows/previsao-ao-vivo.yml"
