# MATLAB handoff — OPCIONAL

Este diretório é um **espelho opcional** do protocolo PREVINE já implementado
em Python (`mimo_core.MimoMLP.fit_previne`).

## Prioridade do projeto

1. **Python** — treino, avaliação e relatório (`run_research_round5.py`+)
2. Artefatos `.mat` Direct — só como **pesos/teto** (inferência), não como runtime de treino
3. Este folder MATLAB — opcional, se alguém quiser reproduzir fora do Python

Não é requisito para a pesquisa MIMO. Não promove modelo ao vivo.

Ver README.md neste mesmo diretório para o script legado.
